apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
  namespace: {{ .Values.nameSpace }}
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
  - protocol: "TCP"
    port: {{ .Values.port }}
    targetPort: {{ .Values.targetPort }}
  type: ClusterIP